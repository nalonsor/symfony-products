{{ form_start(form, { 'attr': {'novalidate': 'novalidate'}}) }}

    {{ form_row(form.name) }}
    {{ form_row(form.description) }}
    {{ form_row(form.size) }}
    {{ form_row(form.save, {'label': 'Save', 'attr': {'class': 'btn btn-primary mt-3'} }) }}

{{ form_end(form) }}